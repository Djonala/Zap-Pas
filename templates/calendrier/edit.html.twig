{% extends 'basecss.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Edit Calendrier{% endblock %}

{% block body %}
    {% include 'navbar/navbaar.html.twig' %}
    <h1 style="text-align: center">Modifier un Calendrier</h1>
        <div style="align-items: center; margin: 0 40%;">
    {{ include('calendrier/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('calendrier/_delete_form.html.twig') }}
        </div>

        <div style="text-align: center">
            <a class="btn-primary btn" href="{{ path('calendrier_index') }}">Retour à l'agenda</a>
        </div>

{% endblock %}
