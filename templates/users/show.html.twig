{% extends 'basecss.html.twig' %}


{% block title %}User{% endblock %}

{% block body %}
    {% include 'navbar/navbaar.html.twig' %}
    <h1>{{ user.nom }} {{ user.prenom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>

        </tbody>
    </table>

    <div style="align-items: center; display: flex; flex-direction : column; margin: 0 40% ;" >
        <a href="{{ path('users_index') }}"><input type="button" class="btn btn-outline-primary" style="margin-bottom: 10px"value="Voir la liste des utilisateurs"></a>
        <a href="{{ path('calendrier_new') }}"><input type="button" class="btn btn-outline-primary" style="margin-bottom: 10px" value="Retour à l'edition calendrier"></a>
        {{ include('users/_delete_form.html.twig') }}
    </div>

{% endblock %}
