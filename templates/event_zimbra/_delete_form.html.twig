<form method="post" action="{{ path('event_zimbra_delete', {'id': event_zimbra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_zimbra.id) }}">
    <button class="btn">Delete</button>
</form>
